{
  "name": "sample-webrtc-client",
  "version": "0.0.1",
  "description": "A sample project for WebRTC client",
  "main": "src/index.ts",
  "scripts": {
    "init": "npm install -g typings && typings install && npm install",
    "build": "$(npm bin)/gulp assets && $(npm bin)/webpack",
    "build:docker": "WS_URL=ws://192.168.99.100:3434 $(npm bin)/gulp assets && $(npm bin)/webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yusuke Goto",
  "license": "MIT",
  "devDependencies": {
    "css-loader": "^0.23.1",
    "extract-text-webpack-plugin": "^1.0.1",
    "gulp": "^3.9.1",
    "gulp-if": "^2.0.1",
    "gulp-uglify": "^1.5.4",
    "postcss-cssnext": "^2.7.0",
    "postcss-import": "^8.1.2",
    "postcss-loader": "^0.9.1",
    "preprocess-loader": "^0.2.0",
    "require-dir": "^0.3.0",
    "style-loader": "^0.13.1",
    "ts-loader": "^0.8.2",
    "typescript": "^1.8.10",
    "webpack": "^1.13.1"
  }
}
